<Window x:Class="SpringPractice2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpringPractice2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel>
            <ComboBox x:Name="currencyBox"/>
            <DatePicker x:Name="startDate"/>
            <DatePicker x:Name="endDate"/>
            <Button x:Name="runBtn" Content="Run" Click="RunBtn_Click"/>
        </StackPanel>

        <DataGrid 
            x:Name="dg"
            Grid.Row="1"
            Grid.ColumnSpan="3">
            
        </DataGrid>
        <local:DataTimeChart x:Name="currencyChart" Grid.Column="1" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
